[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "filecommit"
version = "0.1.0"
description = "Explicit, dependency-free atomic file replacement for Python"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]
authors = [{name = "filecommit contributors"}]
keywords = ["atomic", "filesystem", "durability", "files", "safe-write"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
dev = [
    "build==1.5.0",
    "coverage==7.15.2",
    "mypy==2.3.0",
    "ruff==0.16.0",
]

[project.urls]
Homepage = "https://github.com/matteobroketa/filecommit"
Repository = "https://github.com/matteobroketa/filecommit"
Issues = "https://github.com/matteobroketa/filecommit/issues"
Changelog = "https://github.com/matteobroketa/filecommit/blob/main/CHANGELOG.md"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
filecommit = ["py.typed"]

[tool.ruff]
target-version = "py39"
line-length = 100
extend-exclude = ["build", "dist"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.9"
strict = true
files = ["src/filecommit"]
exclude = ["build", "dist"]

[tool.coverage.run]
branch = true
source = ["filecommit"]

[tool.coverage.report]
fail_under = 95
show_missing = true
skip_covered = true
exclude_also = [
    "if TYPE_CHECKING:",
    "raise AssertionError",
    "raise NotImplementedError",
]
